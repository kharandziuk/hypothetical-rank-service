version: "3.7"

services:
  db:
    image: postgres:11-alpine
    ports:
      - 5432:5432
    env_file:
      - .env

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672
